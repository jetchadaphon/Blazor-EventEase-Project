# EventEase App (Blazor Server)

Minimal Blazor Server demo app implementing:

- Event card component with navigation
- Routing: Home, Event Details, Register, Confirmation
- Registration form with DataAnnotations validation
- State management via scoped `EventStateService` and localStorage

Run:

```powershell
dotnet restore
dotnet run --project EventEase.Server
```
